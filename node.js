import _ from 'ttdvsss';

_.forEach([1, 2, 3], function(value) {
    console.log(value);
});

const _ = require('lodash');

_.forEach([1, 2, 3], function(value) {
    console.log(value);
});
